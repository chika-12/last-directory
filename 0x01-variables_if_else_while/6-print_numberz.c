#include <stdio.h>
/**
 * main - Entry point
 * prints numbers from 0 to 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num;

	for (num = 48; num <= 57; num++)
	{
		putchar(num);
	}
	putchar(10);
	return (0);
}
