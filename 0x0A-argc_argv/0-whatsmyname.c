#include <stdio.h>
#include <stdlib.h>
/**
 * main - A program that prints its name
 * @argc: parameter
 * @argv: parameter
 *
 * Return: Argc and Argv
 */
int main(int argc, char *argv[])
{
	int count;

	if (count >= 0)
	{
		printf("%s\n", argv[0]);
	}
	return (0);
}
